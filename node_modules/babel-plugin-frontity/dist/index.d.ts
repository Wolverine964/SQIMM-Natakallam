import * as BabelTypes from "@babel/types";
import { Visitor } from "@babel/traverse";
interface Babel {
    types: typeof BabelTypes;
}
declare const _default: (babel: Babel) => {
    name: string;
    visitor: Visitor<{}>;
};
export default _default;
